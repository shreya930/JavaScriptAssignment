const vowelCount = (str) => {
    const vowels = ['a','e','i','o','u','A','E','I','O','U'];
    vowels.forEach((element,ind) => {
        console.log(element);
    });
}
str = "Hey this Sreyans Ranjan";
vowelCount(str);